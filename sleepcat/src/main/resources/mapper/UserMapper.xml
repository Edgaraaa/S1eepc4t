<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dozer.sleepcat.dao.dao.UserMapper" >

    <select id="selectByUID" parameterType="int" resultType="User">
        SELECT ID, USERNAME, COUNTRY,TEAMNAME, INTEAM, SELFSCORE WHERE UID=#{uid}
    </select>
    <select id="selectByUserName" parameterType="string" resultType="User">
        SELECT ID, USERNAME, COUNTRY,TEAMNAME, INTEAM, SELFSCORE WHERE username=#{username}
    </select>

</mapper>
